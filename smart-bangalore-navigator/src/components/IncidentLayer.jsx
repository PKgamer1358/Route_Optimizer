// src/components/IncidentLayer.jsx
// Handled directly inside MapView.jsx for simplicity in this MVP to avoid passing leaflet context around unnecessarily.
export default function IncidentLayer() { return null; }
